# This is an example PKGBUILD file. Use this as a start to creating your own,
# and remove these comments. For more information, see 'man PKGBUILD'.
# NOTE: Please fill out the license field for your package! If it is unknown,
# then please put 'unknown'.

# Maintainer: Your Name <youremail@domain.com>
pkgname=apple-t2-audio-config
pkgver=0.2
pkgrel=1
pkgdesc="System configuration files for the apple-bce audio driver"
arch=('any')
url="https://github.com/kekrby/t2-better-audio"
license=('MIT')
replaces=(apple-t2-audio-config-alt)
optdepends=(
  'linux-t2: for audio kernel module'
  'apple-bce: for audio kernel module'
  pulseaudio
  pipewire
)
source=(
  audio::git+https://github.com/kekrby/t2-better-audio#commit=bf5be12f1533f4c6ce77831db16acdaea9b04a22
)

sha256sums=('SKIP')

package() {
	mkdir -p $pkgdir/usr/share/alsa
	cp -rv $srcdir/audio/files/ucm2 $pkgdir/usr/share/alsa
}


